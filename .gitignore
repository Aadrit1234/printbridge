# Dependencies and build output
node_modules/
dist/

# Vercel project link (local ids, not secrets — but not needed in git)
.vercel/

# Runtime data — uploads, generated PDFs, previews, the outbox, jobs and the
# admin PIN hash. Never commit this: it is your documents and your secret.
data/

# Local overrides and logs
.env
.env.local
*.log
server.pid
server.out

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Agent scratch space
.freebuff/

.vercel
